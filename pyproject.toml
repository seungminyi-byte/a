[project]
name = "legal-case-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]",
    "playwright",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
